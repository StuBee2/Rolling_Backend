subprojects {

    bootJar { enabled = false }
    jar { enabled = true }

    dependencies {
        compileOnly project(':Rolling-Domains')
    }

}

bootJar { enabled = false }
jar { enabled = true }